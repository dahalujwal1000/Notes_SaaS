# Pinned to the versions the project was built and tested against.
fastapi==0.141.1
uvicorn==0.52.4
sqlalchemy==2.0.52
pydantic==2.13.5
python-jose[cryptography]==3.5.0
# bcrypt is used directly (passlib 1.7.4 is hard-incompatible with bcrypt 5.x).
bcrypt==5.0.0
python-multipart==0.0.32
# PostgreSQL driver (SQLAlchemy's psycopg 3 dialect) — used when DATABASE_URL
# points at a Postgres database; ignored for local SQLite development.
psycopg[binary]==3.3.4
# Loads a local .env file for local development; hosting platforms set real
# environment variables from their dashboards, which always take precedence.
python-dotenv==1.2.3
# AI assistant: REST calls to the Gemini / Groq / Mistral APIs (also already
# a transitive dependency of FastAPI's TestClient).
httpx==0.28.1
