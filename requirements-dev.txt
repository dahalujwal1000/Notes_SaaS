# Development & test dependencies — not needed for deployment.
# Install with:  pip install -r requirements-dev.txt
pytest==9.1.1
httpx==0.28.1
# Linter — enforced by CI (.github/workflows/ci.yml) and configured in ruff.toml.
ruff==0.16.6
